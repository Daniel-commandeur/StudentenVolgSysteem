@model IEnumerable<StudentenVolgSysteem.Models.Werkvorm>

@{
    ViewBag.Title = "Werkvormen";
}

<h2>Werkvormen</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WerkvormId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WerkvormId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WerkvormId })
        </td>
    </tr>
}

</table>
