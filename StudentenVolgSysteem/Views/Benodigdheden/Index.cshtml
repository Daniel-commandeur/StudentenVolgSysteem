@model IEnumerable<StudentenVolgSysteem.Models.BenodigdheidModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BenodigdheidId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BenodigdheidId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BenodigdheidId })
        </td>
    </tr>
}

</table>
