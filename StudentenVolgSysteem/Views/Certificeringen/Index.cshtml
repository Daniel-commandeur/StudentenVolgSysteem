@model IEnumerable<StudentenVolgSysteem.Models.Certificering>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CertificeringId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CertificeringId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CertificeringId })
        </td>
    </tr>
}

</table>
