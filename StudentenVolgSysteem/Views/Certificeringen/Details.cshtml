@model StudentenVolgSysteem.Models.Certificering

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Certificering</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Naam)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Naam)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CertificeringId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
