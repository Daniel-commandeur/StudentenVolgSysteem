@model IEnumerable<StudentenVolgSysteem.Models.NiveauModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Niveau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Niveau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NiveauId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NiveauId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NiveauId })
        </td>
    </tr>
}

</table>
